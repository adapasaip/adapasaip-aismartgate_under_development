# Backend URL Configuration
# Leave empty if frontend and backend are on the same origin (production)
# Set this if running frontend and backend on different ports during development
# Example: VITE_BACKEND_URL=http://localhost:5000
VITE_BACKEND_URL=
